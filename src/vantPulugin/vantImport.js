import Vue from 'vue'

import {
  Cell,
  CellGroup,
  Card,
  Tabbar,
  Button,
  TabbarItem,
  Icon,
  Tab,
  Tabs,
  TreeSelect,
  Field,
  Stepper,
  SubmitBar,
  Popup,
  NavBar,
  Tag
} from 'vant'

Vue.use(Button)
  .use(Icon)
  .use(Tab)
  .use(Tabs)
  .use(TreeSelect)
  .use(Field)
  .use(Stepper)
  .use(SubmitBar)
  .use(Popup)
  .use(NavBar)
  .use(Cell)
  .use(CellGroup)
  .use(Card)
  .use(Button)
  .use(Tabbar)
  .use(TabbarItem)
  .use(Tag)
