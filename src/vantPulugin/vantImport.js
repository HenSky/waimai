import Vue from 'vue'

import {
  Icon,
  Tab,
  Tabs,
  Cell,
  CellGroup,
  Field,
  Button,
  Tabbar,
  TabbarItem
} from 'vant'

Vue
  .use(Icon)
  .use(Tab).use(Tabs)
  .use(Cell)
  .use(CellGroup)
  .use(Field)
  .use(Button)
  .use(Tabbar)
  .use(TabbarItem)
