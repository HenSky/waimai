import Vue from 'vue';

import {
    Button,
    Tabbar,
    TabbarItem,
    Icon,
    Tab,
    Tabs,
    TreeSelect,
    Field,
    Stepper,
    SubmitBar,
    Popup,
    NavBar
} from "vant";

Vue.use(Button)
    .use(Tabbar)
    .use(TabbarItem)
    .use(Icon)
    .use(Tab)
    .use(Tabs)
    .use(TreeSelect)
    .use(Field)
    .use(Stepper)
    .use(SubmitBar)
    .use(Popup)
    .use(NavBar);