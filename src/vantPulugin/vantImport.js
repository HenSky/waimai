import Vue from 'vue'

import {
  Cell, CellGroup,
  Card,
  Button,
  Tabbar,
  TabbarItem
} from 'vant'

Vue
  .use(Cell).use(CellGroup)
  .use(Card)
  .use(Button)
  .use(Tabbar)
  .use(TabbarItem)
